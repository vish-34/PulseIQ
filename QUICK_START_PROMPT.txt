Build a trauma detection system: Multi-phase emergency response that detects crashes via triangulation (G-force >4G, heart rate spike>140 then drop<50, no voice), then activates 3 parallel agents: Dispatcher (uses Google Maps Places API to find REAL nearest trauma center, gets actual hospital name/address/phone from Google, calls hospital phone from Google or +918454030044 as fallback), Guardian (QR code, dashboard), Treasurer (pre-auth token ₹50k, emails to hospital email from Google Maps or vishal23borana@gmail.com as fallback). Use these exact configs: FAMILY_PHONE=+917738187807, FAMILY_EMAIL=rookiedev.mujahid@gmail.com, HOSPITAL_PHONE=+918454030044 (fallback), HOSPITAL_EMAIL=vishal23borana@gmail.com (fallback), GOOGLE_MAPS_API_KEY=[get from Google Cloud Console], SMTP_HOST=smtp.gmail.com, SMTP_PORT=587, SMTP_USER=rookiedev.mujahid@gmail.com. Build in 6 parts: (1) Schemas + Crash Detector + State Machine, (2) Config + Google Maps (real hospital lookup) + Twilio + Email + QR, (3) Insurance layer, (4) 3 Agents + Master Orchestrator, (5) Controllers + Routes + App with auto-test, (6) Test scripts. CRITICAL: Google Maps finds REAL hospitals, use hospital phone/email from Google if available else use HOSPITAL_PHONE/EMAIL as fallback. Email service must check if SMTP configured FIRST then send real emails. Use Pydantic v2 (pattern= not regex=). All async. Add --auto-test flag to app.py. Final: python run.py auto-triggers test and sends emails to real addresses. Build one part at a time.
